export class ZingoCard {
    cardNumber: number;
    spaces: [ // 3 x 3 layout of spaces on card
        string, string, string,
        string, string, string,
        string, string, string
    ];
}
